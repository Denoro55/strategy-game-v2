import { DRAWER_CONFIG } from './drawer';
import { STAGE_CONFIG } from './stage';
import { INSTANCES_CONFIG } from './instances';
import { ENV } from './env';

export const CONFIG = {
  env: ENV,
  drawer: DRAWER_CONFIG,
  stage: STAGE_CONFIG,
  instances: INSTANCES_CONFIG,
};
